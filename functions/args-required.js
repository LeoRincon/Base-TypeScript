"use strict";
(function () {
    // param obligatorios
    var fullName = function (firstName, lastName) {
        return firstName + " " + lastName;
    };
    var name = fullName('leo', 'rincon');
    console.log(name);
})();
