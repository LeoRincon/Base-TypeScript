(() => {
  //! void === vacio se usa para decir que no retorna nada

  function callBatman(): void {
    //code
    // pero especificamos que no retorna nada.
  }

  const a = callBatman();
  console.log(a);

  const callSuperMan = (): void => {
    //code
  };
})();
